# ── Backend (FastAPI / Render or Railway) ─────────────────────────────────────
# Set this to your deployed Netlify frontend URL (no trailing slash).
FRONTEND_URL=https://your-site.netlify.app

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dbname
