# ── Frontend (Vite / Netlify) ─────────────────────────────────────────────────
# Set this to your deployed backend URL (no trailing slash).
# Leave blank in local dev — Vite proxy handles it automatically.
VITE_API_URL=https://your-backend.onrender.com
