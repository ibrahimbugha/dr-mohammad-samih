# OrthoAnalyse — Static HTML deployment
# No build step required. Netlify serves index.html directly.

[build]
  publish = "."

# Send all routes to index.html (SPA-style navigation)
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
